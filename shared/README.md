# Shared Directory

Use this folder for reusable, domain-agnostic building blocks.

Examples:

- `shared/ui`: generic UI components
- `shared/api`: HTTP client and request helpers
- `shared/lib`: utility functions
- `shared/config`: environment/config modules
- `shared/types`: common cross-feature types
