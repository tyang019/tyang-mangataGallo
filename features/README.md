# Features Directory

Use this folder for business/domain modules.

Each feature should keep its own API access, models/types, state, hooks, and UI together.

Example structure:

```text
features/
  catalog/
    api/
    model/
    ui/
    hooks/
```
